import { AdminLayout } from "@/components/layout/admin-layout";
import { useListInventoryAlerts } from "@workspace/api-client-react";
export default function AdminAlerts(){const q=useListInventoryAlerts();return <AdminLayout><div className="p-6 md:p-10 space-y-6 overflow-auto"><h1 className="text-3xl font-bold">Alertas de inventario</h1><div className="overflow-x-auto rounded-lg border bg-card"><table className="w-full text-sm"><thead className="bg-muted/50"><tr>{["Fecha","Producto","SKU","Sucursal","Stock","Mínimo","Tipo","Responsable","Entrega"].map(x=><th className="p-3 text-left" key={x}>{x}</th>)}</tr></thead><tbody>{q.data?.map((a:any,i:number)=><tr className="border-t" key={a.id||i}><td className="p-3">{a.createdAt||a.date?new Date(a.createdAt||a.date).toLocaleString("es-MX"):"—"}</td><td className="p-3">{a.productName||a.product?.name||"—"}</td><td className="p-3 font-mono">{a.sku||"—"}</td><td className="p-3">{a.branchName||a.branch?.name||"—"}</td><td className="p-3">{a.stock??a.inventory??"—"}</td><td className="p-3">{a.minimum??a.minStock??"—"}</td><td className="p-3">{a.type||a.alertType||"—"}</td><td className="p-3">{a.responsible||"—"}</td><td className="p-3">{a.deliveryState||a.status||"—"}</td></tr>)}</tbody></table>{!q.data?.length&&<p className="p-8 text-center text-muted-foreground">No hay alertas registradas.</p>}</div></div></AdminLayout>}